sudo: required
services:
  - docker
script:
  - docker run -e CI=true leslieturis/rbp-docker-test npm run test -- --coverage
before_install:
  - docker build -t leslieturis/rbp-docker-test -f Dockerfile.dev .
